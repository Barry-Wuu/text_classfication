# 张量/中间产物（体积大，可现场重建，不入库）
resources/*.npz
resources/*.npy
resources/*.pkl
resources/*.pt

# 评测输出（临时结果）
eda/ab_result.txt
eda/ab_speed_svm.txt
resources/tfidf_clf_result.json

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# WorkBuddy 会话工作数据（项目内保留，不入库）
.workbuddy/

# 部署用量化权重（体积大，走 GitHub Release 分发）
deploy/models/*.pt

# 降维可视化的中间缓存（可由脚本重建）
eda/_cache/
